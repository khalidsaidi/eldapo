# .ai Workspace

This directory is reserved for agent-only artifacts:
- plans
- temporary notes
- logs
- scratch outputs

Rules:
- Do not place application source code in `.ai/`.
- Keep most `.ai/` contents untracked.
- Only `.ai/README.md` and `.ai/.gitkeep` should normally be committed.
